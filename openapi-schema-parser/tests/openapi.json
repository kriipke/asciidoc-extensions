paths:
  /users:
    get:
      summary: List users
      description: Returns all users.
      parameters:
        - name: limit
          in: query
          description: Max number of users to return
      responses:
        "200":
          description: A list of users
